.env
/secrets/

.direnv

# cdk8s / generator
generator/dist/
generator/.venv/
generator/imports/
.direnv/manifests/
.direnv/compare/

__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
pip-log.txt
pip-delete-this-directory.txt
.tox/
.coverage
.mypy_cache/
.pytest_cache/
*.egg-info/
build/
dist/

# Nix
result
.devenv

# Vendored submodules / repos
vendor/
# Deliberately tracked, checksum-locked selective MiaAI source snapshots.
!llm-test/lanes/*/vendor/
!llm-test/lanes/*/vendor/**
# Native runtime artifacts must be staged out-of-band, never vendored in Git.
llm-test/lanes/*/vendor/**/*.so

# Snapshots (for migration verification)
snapshot-baseline/
snapshot-cdk8s/

# pi session artifacts
.pi-subagents/

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../12-dsv41-parity.yaml
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: dsv41-exl3-stock-matched-profile
    namespace: llm-test
    files:
      - profile.env=../serving/profile.env
patches:
  - path: deployments-patch.yaml

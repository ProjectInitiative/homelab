apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: placeholder-secret
spec:
  vaultAuthRef: placeholder-auth
  mount: secret
  type: Opaque
  path: placeholder/path
  destination:
    name: placeholder-dest
    create: true

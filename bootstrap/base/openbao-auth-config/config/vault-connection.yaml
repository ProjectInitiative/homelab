apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultConnection
metadata:
  name: default
  namespace: openbao-secrets-operator
spec:
  address: http://openbao-ts.taildeab2.ts.net
  skipTLSVerify: true # This should be set to false in a production environment

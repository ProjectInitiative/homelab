---
apiVersion: v1
kind: Service
metadata:
  name: mcp-ts-placeholder
  labels:
    app: mcp-kubernetes
  annotations:
    tailscale.com/proxy-group: ingress-proxy
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  ports:
    - name: http-sse
      protocol: TCP
      port: 80
      targetPort: 8080
  # The MCP SSE transport keeps sessions in process memory. Keep the SSE
  # connection and subsequent /message requests on the same backend pod.
  sessionAffinity: ClientIP
  selector:
    app: mcp-kubernetes

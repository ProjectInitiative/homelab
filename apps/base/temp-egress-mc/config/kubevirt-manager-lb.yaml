apiVersion: v1
kind: Service
metadata:
  name: ts
  namespace: kubevirt-manager
spec:
  selector:
    app: kubevirt-manager
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
  loadBalancerClass: tailscale
